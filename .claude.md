# Muse — project context

Local macOS Electron app for collecting, organizing, and searching design/image files. React 19 renderer, TypeScript everywhere, SQLite via better-sqlite3, plus a **bundled Ollama** for AI features (Qwen3-VL for vision/captioning, nomic-embed-text for caption embeddings; sqlite-vec for KNN, perceptual hash + palette indexing for visual signals).

---

## Commands

| Command | Purpose |
|---------|---------|
| `npm install` | Install dependencies |
| `npm start` | Dev: Electron Forge + Vite (renderer hot-reload; restart for main/preload changes) |
| `npm run lint` | ESLint (`*.ts`, `*.tsx`) — the only check script |
| `npm run package` | Forge package (unsigned `.app`) |
| `npm run make` | Package + `MakerZIP` (darwin) + custom `postPackage` hook that builds `out/make/Muse-Installer.pkg` |

**Ollama is managed by the app, not the user.** `src/main/ai/ollama-server.ts` spawns a server on `127.0.0.1:11434` with `OLLAMA_MODELS` pointed at `userData/ollama-models`. Binary resolution: packaged → `process.resourcesPath/ollama` (shipped via `extraResource`, gitignored in-tree); dev → `/opt/homebrew/bin/ollama`. If a server is already running externally, the app attaches to it instead of spawning.

Models (`qwen3-vl:8b-instruct` ≈6 GB, `nomic-embed-text` ≈274 MB) are pulled **in-app** on first run by `ModelSetup.tsx` via `ollama:pullModel` — no manual `ollama pull` needed.

---

## Repository index

### Root

| Path | Role |
|------|------|
| `forge.config.ts` | Forge + Vite pipeline; `postPackage` prunes to prod deps, rebuilds natives for Electron 41.5.0 (arm64), then `pkgbuild`/`productbuild` an unsigned installer |
| `vite.*.config.ts` | Separate Vite configs: main / preload / renderer |
| `index.html` | Renderer HTML entry |
| `tsconfig.json` / `.eslintrc.json` | TS + ESLint |
| `resources/` | App icon, `scripts/postinstall` (strips quarantine), `ollama/` binary (gitignored) |
| `scripts/*.mjs` | Standalone `node` diagnostics/verification against a **copy** of the library DB (search pipeline, moodboards, palettes, migrations). Not wired into npm scripts |

### Main process (`src/main/`)

| Path | Role |
|------|------|
| `index.ts` | Lifecycle: `BrowserWindow`, `local-file` protocol, DB init, app menu, theme/dialog/update IPC, squirrel startup, staggered launch jobs |
| `ipc-handlers.ts` | Registers domain IPC (folders, images, tags, colors, import, AI, library, clipboard, ollama) **and owns the in-memory AI queue** |
| `importer.ts` | Import from file paths, URL, or raw buffer → originals + thumbnails |
| `color-extractor.ts` | Palette extraction, thumb color index (hue buckets/chromatic), palette backfill |
| `library-export.ts` | Snapshot DB + assets into a `.muse` zip bundle |
| `library-import.ts` | Inspect (duplicate detection) → per-image replace/keep decisions → apply; sessions are cancellable and stale ones reaped |
| `updater.ts` | GitHub Releases check, `.pkg` download w/ progress, `installAndRestart` |
| `database/connection.ts` | SQLite open (WAL, FK on), `sqlite-vec` load, migrations |
| `database/schema.ts` | Tables + `runMigrations` (see Schema below) |
| `database/similarity-prefs.ts` | Similarity prefs persisted in `settings` (`visual_similarity_prefs`) |
| `database/repositories/{folders,images,tags}.ts` | CRUD, filtering, counts, trash |
| `ai/ollama-server.ts` | Spawn/stop bundled Ollama, model availability, streaming pulls |
| `ai/ollama-client.ts` | `VISION_MODEL`, embeddings (task-prefixed), caption/describe, moodboard intent parsing, per-image fit scoring |
| `ai/auto-tagger.ts` | Vision auto-tagging; `CAPTIONS_VERSION` gates re-captioning |
| `ai/embeddings.ts` | Compose caption text, store/normalize vectors, `upgradeEmbeddingIndexIfNeeded` |
| `ai/phash.ts` | 64-bit perceptual hash via sharp + Hamming/similarity helpers |
| `ai/prompt-color.ts` | Parse color intent from a prompt and score it against a palette |
| `ai/natural-search.ts` | Text search, moodboard search, find-similar orchestration |

### Shared (`src/shared/`)

Imported by **both** main and renderer — keep it electron-free.

| Path | Role |
|------|------|
| `visual-similarity.ts` | Match-strength tiers (`broad`/`balanced`/`strict`) → min cosine, labels, likeness % formatting |
| `image-color-index.ts` | 12-bin hue ring math, dominant-hue axis/dual-hue boosts |
| `similar-refine.ts` | `SimilarRefineMode` union (currently just `'colors'`) + parser |

### Preload (`src/preload/`)

`preload.ts` — `contextBridge.exposeInMainWorld('electronAPI', …)`, exports the `ElectronAPI` type. Handles drops itself: local files via `webUtils.getPathForFile`, browser drags via `text/uri-list` → `x-moz-url` → HTML `<img src>` → plain text fallback, then `import:files` / `import:url`. Also caches `os.userInfo().username` for the app title.

### Renderer (`src/renderer/`)

| Path | Role |
|------|------|
| `main.tsx` / `App.tsx` | React bootstrap; `App` renders `AppShell` + `ModelSetup` |
| `index.css` | Tailwind 4 / global styles |
| `stores/app-store.ts` | Zustand store: selection, view mode, grid/sidebar sizing, search, similar-images state + prefs |
| `lib/ipc.ts` | `api` alias for `window.electronAPI` + typing |

**Components**

| Area | Files |
|------|-------|
| Layout | `layout/AppShell.tsx`, `EmbeddingProgress.tsx`, `ModelSetup.tsx`, `ModelUpdateDialog.tsx`, `UpdateDialog.tsx`, `MoodboardDialog.tsx`, `LibraryExport.tsx`, `LibraryImportDialog.tsx` |
| Sidebar | `sidebar/Sidebar.tsx` |
| Grid / browse | `grid/ContentGrid.tsx`, `ImageCard.tsx`, `ImageFocus.tsx` |
| Detail | `detail/DetailPanel.tsx`, `SimilarImagesStrip.tsx`, `SimilarityInspectorPopover.tsx` |

---

## Architecture (data flow)

1. **Renderer** calls `window.electronAPI.*` (`src/renderer/lib/ipc.ts`).
2. **Preload** forwards via `ipcRenderer.invoke` / `on` (`src/preload/preload.ts`).
3. **Main** handles in `ipc-handlers.ts` and `index.ts` (dialogs, theme, update, log, drop echo).

**Library location:** `app.getPath('userData')/library/` — `library.db`, `originals/`, `thumbnails/` (created on demand by `getLibraryPath()`). Ollama models live beside it in `userData/ollama-models`.

**Staggered launch jobs** in `index.ts` (all non-fatal, all resumable next launch):

| Delay | Job |
|-------|-----|
| 5s | `backfillMissingPalettes` — palette rows for older imports |
| 8s | `upgradeEmbeddingIndexIfNeeded` — re-embed when the index format changes; reports through `embedding:progress` |
| 10s | `queueCaptionUpgradeIfNeeded` — re-caption images below `CAPTIONS_VERSION` through the normal AI queue |
| 12s | `checkForUpdate` — packaged builds only; silent, emits `update:available` |

---

## Schema (`database/schema.ts`)

`folders`, `tags`, `images`, `image_colors`, `image_tags`, `settings`, FTS5 `images_fts`, and vec0 `image_embeddings` (768-dim, nomic).

Migrated-in `images` columns worth knowing: `phash BLOB`, `alt_text`, `captions_version`, and the color index set `indexed_chromatic`, `indexed_hue_bucket`/`_2`, `indexed_hue_strength`/`_2`, `indexed_hue_degrees`.

`smart_folders` and `ai_queue` exist in the schema but **nothing reads or writes them** — the AI queue is an in-memory array in `ipc-handlers.ts`. Don't assume DB-backed queue semantics.

---

## IPC surface (channel → responsibility)

**Folders:** `folders:getAll`, `folders:create`, `folders:update`, `folders:delete`

**Images:** `images:query`, `images:getById`, `images:update`, `images:trash`, `images:restore`, `images:delete`, `images:restoreAll`, `images:emptyTrash`, `images:getCounts`

**Tags:** `tags:getAll`, `tags:create`, `tags:delete`, `tags:addToImage`, `tags:removeFromImage`, `tags:getForImage`, `tags:getTotalCount` *(registered in main, not exposed in preload)*

**Colors:** `colors:getForImage`

**Import:** `import:files`, `import:url`, `import:buffer`, `dialog:openFiles`

**AI:** `ai:reanalyzeImages`, `ai:analyzableCount`, `ai:reanalyzeAll`, `ai:searchByText`, `ai:searchForMoodboard`, `ai:findSimilar`, `embeddings:hasForImage`

**Similarity prefs:** `settings:getSimilarityPrefs`, `settings:setSimilarityPrefs`

**Ollama:** `ollama:isServerRunning`, `ollama:isModelReady`, `ollama:pullModel`

**Library bundles:** `dialog:saveLibraryBundle`, `dialog:openLibraryBundle`, `library:export`, `library:inspectImport`, `library:applyImport`, `library:cancelImport`

**App update:** `update:check`, `update:download`, `update:install`

**Misc:** `app:getDataPath`, `theme:get`, `clipboard:copyImage`, `ipcMain.on('log')`, `ipcMain.on('files-imported')`

**Main → renderer events:** `theme:changed`, `files-imported`, `embedding:progress`, `autotag:progress`, `moodboard:progress`, `ollama:pullProgress`, `library:export:progress`, `library:import:progress`, `update:available`, `update:downloadProgress`, and menu events `menu:importFiles` / `menu:exportLibrary` / `menu:importLibrary` / `menu:updateModel` / `menu:checkUpdate`.

---

## Conventions / pitfalls

- **Main vs renderer:** Changing `src/main/**` or `src/preload/**` requires a full app restart; the renderer hot-reloads via Vite.
- **Dropped files:** Paths must come from preload (`webUtils.getPathForFile`) — not from renderer-only APIs. The drop listener lives in preload, not React.
- **Local images:** `local-file://` is registered privileged (`bypassCSP`, `stream`, `supportFetchAPI`) and streamed from disk in `src/main/index.ts`.
- **AI queue:** One in-memory queue with a single drainer in `ipc-handlers.ts`. New work extends the in-flight totals rather than starting a second drainer; counters reset only when it fully drains. Preserve that invariant when adding AI work.
- **Embeddings:** nomic requires task prefixes — corpus text uses `search_document: `, queries use `search_query: ` (`EMBED_DOCUMENT_PREFIX` / `EMBED_QUERY_PREFIX`). Mixing them silently degrades results and is what `upgradeEmbeddingIndexIfNeeded` exists to repair.
- **Caption generation:** Bump `CAPTIONS_VERSION` in `auto-tagger.ts` when the vision model or prompt changes — that's what triggers the launch-time re-caption sweep.
- **AI degradation:** Every AI path must stay optional; the app works with Ollama unavailable.
- **New menu items:** Add to `buildAppMenu()` in `index.ts`, forward through `onMenuEvent` in preload, and widen its channel union.
- **Preload isolation:** Preload must never import from `src/main/**` (it would pull in `electron.app`). Duplicate the type locally, as `UpdateInfo` does, or put it in `src/shared/`.
- **Packaging:** `asar: false`, native modules rebuilt in `postPackage` against a hardcoded Electron version — bump it in `forge.config.ts` alongside the `electron` devDependency.

---

## When editing

Prefer extending existing repositories and IPC naming patterns. Keep thumbnail/color extraction non-fatal where already wrapped. Match the Zustand + `components/` layout rather than introducing parallel state layers unless necessary. Logic shared between main and renderer belongs in `src/shared/`.
